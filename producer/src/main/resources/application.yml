demo:
  service:
    version: 1.0.0

embedded:
  zookeeper:
    port: 2181
  redis:
    port: 6379

dubbo:
  protocol:
    name: dubbo
    port: -1
  registry:
    address: redis://127.0.0.1:${embedded.redis.port}
    password: fulihui.com
    username: fulihui.com
  scan:
    base-packages: com.fulihui.redisdubbo.demo.producer.service

spring:
  application:
    name: dubbo-registry-redis-provider
server:
  port: 8081